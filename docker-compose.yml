services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - 3000
      - 3001
    container_name: smgames
    environment:
      - NODE_ENV=production
      - PORT=3000
      - WS_PORT=3001
    restart: unless-stopped
